package GameRPG.Characters;

public class PlayerCharacter extends Character{

    public PlayerCharacter(int hp, int attackPower, String name, int experience, int level, int armour, int maxHp, int skillPoints) {
        super(hp, attackPower, name, experience, level, armour, maxHp, skillPoints);
    }
}
